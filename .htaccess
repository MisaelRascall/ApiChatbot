# Activar reescritura de URLs
RewriteEngine On
RewriteBase /ApiChatbot/

# Excluir archivos y carpetas existentes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir todo lo demás a api.php
RewriteRule ^(.*)$ public/api.php [QSA,L]
